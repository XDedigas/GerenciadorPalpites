@model GerenciadorPalpites.Web.Models.UsuarioViewModel
@{
    ViewBag.Title = "Cadastrar-se";
}

<style>
    .container-tela {
        width: 400px;
        margin: 0 auto;
    }
</style>

<div class="container-tela">
    <div class="row">
        <h3 class="titulo">@ViewBag.Title</h3>
    </div>
    <div class="row">
        <section>
            @using (Html.BeginForm("CadastrarSe", "Conta", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
            {
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                <div class="form-group">
                    @Html.LabelFor(m => m.Nome, new { @class = "control-label" })
                    @Html.TextBoxFor(m => m.Nome, new { @class = "form-control" })
                    @Html.ValidationMessageFor(m => m.Nome, "", new { @class = "text-danger" })
                </div>
                <div class="form-group">
                    @Html.LabelFor(m => m.Email, new { @class = "control-label" })
                    @Html.TextBoxFor(m => m.Email, new { @class = "form-control" })
                    @Html.ValidationMessageFor(m => m.Email, "", new { @class = "text-danger" })
                </div>
                <div class="form-group">
                    @Html.LabelFor(m => m.Login, new { @class = "control-label" })
                    @Html.TextBoxFor(m => m.Login, new { @class = "form-control" })
                    @Html.ValidationMessageFor(m => m.Login, "", new { @class = "text-danger" })
                </div>
                <div class="form-group">
                    @Html.LabelFor(m => m.Senha, new { @class = "control-label" })
                    @Html.PasswordFor(m => m.Senha, new { @class = "form-control" })
                    @Html.ValidationMessageFor(m => m.Senha, "", new { @class = "text-danger" })
                </div>

                if (ViewBag.Mensagem != null)
                {
                    if (ViewBag.Mensagem[0] == "ok")
                    {
                        <div class="alert alert-success">
                            <strong>Sucesso!</strong> @ViewBag.Mensagem[1]
                        </div>
                    }
                    else
                    {
                        <div class="alert alert-warning">
                            <strong>Aviso!</strong> @ViewBag.Mensagem[1]
                        </div>
                    }
                }

                <div class="form-group">
                    <input type="submit" class="btn btn-success" value="Cadastrar" />
                </div>
            }
        </section>
    </div>
</div>

@section Scripts{
    @Scripts.Render("~/bundles/jqueryval")
}